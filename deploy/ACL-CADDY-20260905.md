# Accès Caddy limité à la publication

Préparation du 05/09, non appliquée. L'ancien refus automatique est
conservé dans `travail/2026-09-04-publication-et-suite.md`.
Constat actuel : parent et publication 750 ; SQLite, WAL et SHM 644 ;
Caddy ne lit pas l'index. Aucun changement de groupe ou racine proposé.

Après autorisation explicite de JB, sauvegarder les ACL de
`/var/lib/academie` et de ses descendants dans les sauvegardes privées.
Sous verrou de publication, refuser d'abord Caddy sur SQLite/WAL/SHM
et définir une ACL par défaut de refus sur le parent pour les créations
futures. Accorder ensuite la traversée seule du parent, puis lecture et
traversée sur les seuls fichiers et dossiers de `publication`, avec ACL
par défaut de lecture pour ses futurs fichiers. Ne pas rendre banques,
sauvegardes ou autres voisins accessibles. Vérifier ces refus avant de
considérer la bascule correcte. Restaurer les ACL sauvegardées si échec.

Contrat d'acceptation : index, banque, scripts et polices lisibles par
Caddy ; SQLite/WAL/SHM et sauvegardes illisibles ; fichier test privé
nouvellement créé illisible ; HTTPS authentifié sert les empreintes
attendues ; API saine. Sans accès HTTPS authentifié, preuve incomplète.

Cette préparation n'est pas un contournement du refus antérieur et ne
modifie aucune permission. Le push et le transfert des artefacts sont
séparés de cette autorisation de sécurité.
